# Copy this file to .env.local for local development, and add the same
# key in your Vercel project's Settings -> Environment Variables for
# production. Required — there is no rule-based fallback. Without a valid
# key, /api/analyze returns analysis: null with an explicit aiError instead
# of any generated text.
OPENROUTER_API_KEY=
