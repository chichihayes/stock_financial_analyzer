# Copy this file to .env.local for local development, and add the same
# key in your Vercel project's Settings -> Environment Variables for
# production. The app works without it (falls back to rule-based analysis),
# but AI-generated analysis requires a valid OpenRouter key.
OPENROUTER_API_KEY=
